version: '3'

services:
  app:
    build:
      context: .
      dockerfile: './Dockerfile'
    volumes:
      - './:/var/www/app'
      - './node_modules:/var/www/app/node_modules'
    ports:
      - 3000:3000
